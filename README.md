# playlist_tracker

when run, will update a csv file with the amount of followers a specified spotify playlist has

csv file should already be created

config file containing values for id and secret key is needed
